import FilterList from '../filterlist/FilterList'
import TicketList from '../ticketlist/TicketList'

function Main() {
  return (
    <main>
      <FilterList />
      <TicketList />
    </main>
  )
}

export default Main
